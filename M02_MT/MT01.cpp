// 打印数组全排列
#include <iostream>
#include <vector>
using namespace std;

void printVector(vector<int>& nums) {
    if (nums.size() == 1) {
        cout << nums[0] << " ";
    } else {
        for (int i = 1; i < nums.size(); ++i) {

        }
    }
}

int main() {
    vector<int> nums;


    return 0;
}

// 后续换成查找数组最大子序列和